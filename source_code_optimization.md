# go-wrk 源代码优化方案

## 已识别的性能瓶颈

### 1. HTTP客户端创建（loader/client.go）
- 每个goroutine都创建自己的HTTP客户端
- 相同的配置被重复创建
- 可以复用HTTP客户端或使用连接池

### 2. URL转义处理（loader/loader.go - escapeUrlStr）
- 每次请求都进行URL查询参数转义
- 字符串操作较多，有分配开销
- 可以缓存转义结果或优化算法

### 3. 请求对象创建（loader/loader.go - DoRequest）
- 每次请求都创建新的http.Request对象
- 头部信息重复设置
- 可以复用请求对象或使用对象池

### 4. 响应体处理（loader/loader.go - DoRequest）
- 使用io.ReadAll读取整个响应体
- 分配内存较大，GC压力大
- 可以流式处理或限制读取大小

### 5. 统计收集（go-wrk.go 和 loader/loader.go）
- 使用通道进行统计聚合
- 可能有锁竞争和上下文切换开销
- 可以使用原子操作或无锁数据结构

### 6. 内存分配模式
- 频繁的字符串操作和切片创建
- 缺少对象复用机制
- 可以引入sync.Pool减少分配

## 优化策略

### 阶段1：低风险优化（立即实施）
1. HTTP客户端复用
2. 请求对象池化
3. 优化URL转义逻辑
4. 减少不必要的内存分配

### 阶段2：中等风险优化（需要测试）
1. 响应体处理优化
2. 统计收集优化
3. 并发模型调整

### 阶段3：高风险优化（架构级）
1. 自定义HTTP传输层
2. 零拷贝网络处理
3. 事件驱动模型

## 具体优化实施

### 1. HTTP客户端复用
```go
// 当前：每个goroutine创建自己的客户端
// 优化：全局共享客户端或客户端池
var clientPool sync.Pool
```

### 2. 请求对象池化
```go
// 使用sync.Pool复用http.Request对象
var requestPool sync.Pool
```

### 3. URL转义优化
```go
// 缓存转义结果，避免重复计算
var urlCache sync.Map
```

### 4. 响应体读取优化
```go
// 限制读取大小或使用缓冲区复用
const maxBodySize = 1024 * 1024 // 1MB
```

### 5. 统计收集优化
```go
// 使用原子操作替代通道
var totalRequests atomic.Int64
var totalErrors atomic.Int64
```

## 预期性能提升

| 优化项目 | 预期提升 | 风险等级 |
|---------|---------|----------|
| HTTP客户端复用 | 10-15% | 低 |
| 请求对象池化 | 5-10% | 低 |
| URL转义优化 | 3-5% | 低 |
| 响应体读取优化 | 5-10% | 中 |
| 统计收集优化 | 3-8% | 中 |
| 内存分配优化 | 8-12% | 低 |
| **综合优化** | **30-50%** | **中** |

## 实施步骤

1. 创建基准测试环境
2. 实施阶段1优化
3. 测试验证性能提升
4. 实施阶段2优化
5. 全面测试和验证
6. 性能分析和调优

## 测试验证

### 基准测试方法
```bash
# 优化前基准
go build -o go-wrk-baseline
./go-wrk-baseline -c 1000 -d 30 http://localhost:8080

# 优化后测试
go build -o go-wrk-optimized
./go-wrk-optimized -c 1000 -d 30 http://localhost:8080
```

### 监控指标
1. QPS (Requests/sec)
2. 内存分配次数和大小
3. GC停顿时间
4. CPU使用率
5. 延迟分布

## 风险控制

### 低风险优化
- HTTP客户端复用：配置一致，线程安全
- 对象池化：标准sync.Pool使用
- 算法优化：逻辑不变，性能提升

### 中风险优化
- 统计收集：需要确保数据一致性
- 响应体处理：可能影响功能完整性

### 高风险优化
- 架构变更：需要全面测试
- 自定义传输层：兼容性风险

## 下一步行动

1. 立即实施阶段1优化
2. 创建性能测试套件
3. 逐步实施后续优化
4. 持续监控和调优
