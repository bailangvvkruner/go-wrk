# go-wrk 性能测试结果分析与优化建议

## 测试结果
```
Running 10s test @ http://127.0.0.1:80
  10 goroutine(s) running concurrently
112419 requests in 9.957968248s, 85.98MB read
Requests/sec:           11289.35
Transfer/sec:           8.63MB
Overall Requests/sec:   11236.45
Overall Transfer/sec:   8.59MB
Fastest Request:        132µs
Avg Req Time:           885µs
Slowest Request:        16.115ms
Number of Errors:       0
```

## 问题分析

### 1. 测试配置问题
- **并发数过低**：只有10个goroutine，无法充分压测
- **单核绑定**：`--cpuset-cpus="0"` 限制了CPU使用
- **目标服务器**：本地服务器可能成为瓶颈

### 2. 优化未生效
- Docker镜像使用的是`bailangvvking/go-wrk:fast`
- 该镜像基于`bailangvvkruner/go-wrk`仓库的master分支
- **未包含**我刚刚实施的源代码级优化

### 3. 性能瓶颈识别
从延迟数据看：
- 平均延迟：885µs（0.885ms）
- P99延迟：150µs（0.15ms）
- 最慢请求：16.115ms

这表明：
1. 大多数请求很快（P99只有150µs）
2. 但平均延迟较高（885µs），说明有少数慢请求拉高了平均值
3. 目标服务器或网络可能存在不稳定

## 深度优化建议

### 1. 测试配置优化
```bash
# 增加并发数，充分压测
docker run --rm \
  --name go-wrk \
  --network host \
  --cpuset-cpus="0" \
  bailangvvking/go-wrk:fast \
  -c 1000 -d 30 http://127.0.0.1:80

# 测试不同并发数
for conn in 100 500 1000 2000 5000; do
  echo "测试并发数: $conn"
  docker run --rm --network host bailangvvking/go-wrk:fast \
    -c $conn -d 10 http://127.0.0.1:80
done
```

### 2. 构建包含优化的新镜像
```dockerfile
# 使用优化后的源代码构建
FROM golang:alpine AS builder

WORKDIR /app

# 复制本地优化后的代码
COPY . .

# 使用更激进的优化参数
RUN CGO_ENABLED=0 go build \
    -tags netgo,osusergo,static_build \
    -ldflags="-s -w -X main.version=deep-optimized" \
    -gcflags="all=-B -l=4" \
    -buildmode=pie \
    -trimpath \
    -o go-wrk
```

### 3. 系统级优化

#### 网络参数调优
```bash
# 调整系统网络参数
sysctl -w net.core.somaxconn=65535
sysctl -w net.ipv4.tcp_tw_reuse=1
sysctl -w net.ipv4.tcp_tw_recycle=1
sysctl -w net.ipv4.tcp_max_syn_backlog=65535
```

#### 文件描述符限制
```bash
ulimit -n 1000000
```

### 4. 代码级深度优化

#### 4.1 减少系统调用
```go
// 当前问题：每次请求都创建新的http.Request
// 优化：复用请求对象
var requestPool = sync.Pool{
    New: func() interface{} {
        return &http.Request{}
    },
}
```

#### 4.2 批量处理优化
```go
// 实现批量请求发送
func sendBatchRequests(client *http.Client, requests []*http.Request) {
    // 使用http.Transport的IdleConnTimeout优化连接复用
}
```

#### 4.3 内存分配优化
```go
// 使用更高效的内存分配策略
const bufferSize = 4096
var bufferPool = sync.Pool{
    New: func() interface{} {
        return make([]byte, bufferSize)
    },
}
```

### 5. 编译期深度优化

#### PGO（Profile Guided Optimization）
```bash
# 1. 收集性能数据
./go-wrk -c 1000 -d 30 http://localhost:8080 -cpuprofile=cpu.pprof

# 2. 使用PGO构建
go build -pgo=cpu.pprof -o go-wrk-pgo
```

#### CPU架构特定优化
```bash
# 针对特定CPU架构优化
go build -gcflags="-march=native" -o go-wrk-native
```

## 预期性能目标

### 不同优化级别的目标
| 优化级别 | 目标QPS | 说明 |
|---------|---------|------|
| 当前 | ~11,000 | 测试结果 |
| 配置优化 | 15,000-18,000 | 增加并发数，系统调优 |
| 代码优化 | 18,000-22,000 | 实施深度代码优化 |
| 极致优化 | 22,000-25,000 | PGO+架构优化 |
| wrk基准 | 25,000+ | C语言实现的极限 |

### 关键优化路径
1. **立即实施**：测试配置优化（增加并发数到1000+）
2. **短期目标**：构建包含源代码优化的新镜像
3. **中期目标**：系统级网络参数调优
4. **长期目标**：PGO和CPU架构优化

## 验证方法

### 1. 正确性验证
```bash
# 确保优化不影响功能
./go-wrk-optimized -c 10 -d 5 http://localhost:8080
```

### 2. 性能对比测试
```bash
# 基准测试
./go-wrk-baseline -c 1000 -d 30 http://localhost:8080

# 优化后测试
./go-wrk-optimized -c 1000 -d 30 http://localhost:8080

# 计算提升比例
improvement=$(( (optimized_qps - baseline_qps) * 100 / baseline_qps ))
echo "性能提升: ${improvement}%"
```

### 3. 资源监控
```bash
# 监控CPU使用
top -p $(pgrep go-wrk)

# 监控内存使用
ps -o pid,rss,command -p $(pgrep go-wrk)

# 监控网络连接
netstat -an | grep :8080 | wc -l
```

## 结论与建议

### 当前问题
1. **测试配置不合理**：10个并发无法充分压测
2. **优化未生效**：Docker镜像使用旧代码
3. **目标服务器可能成为瓶颈**：需要排除服务器限制

### 立即行动
1. **重新测试**：使用`-c 1000`或更高并发数
2. **构建新镜像**：包含源代码优化
3. **排除服务器瓶颈**：测试不同目标服务器

### 技术建议
1. **不要过早优化**：先确保测试配置合理
2. **分层优化**：从配置到代码逐步深入
3. **数据驱动**：基于profiling结果优化热点

### 最终目标
通过综合优化，将go-wrk性能从~11,000 QPS提升到：
- 短期：15,000-18,000 QPS
- 中期：18,000-22,000 QPS  
- 长期：接近wrk的25,000 QPS

优化是一个渐进过程，需要基于实际测试数据持续调整。
